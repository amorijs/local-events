import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import SampleContainer from './components/SampleContainer.jsx';

ReactDOM.render(<SampleContainer />, document.getElementById('app'));
